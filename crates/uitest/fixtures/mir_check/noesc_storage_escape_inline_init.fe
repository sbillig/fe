// NoEsc escapes should be diagnosed even if the value is constructed inline.

struct Esc {
    h: mut u256,
    tag: u256,
}

pub contract C {
    mut slot: Esc,

    init() uses (mut slot) {
        let mut x: u256 = 0
        slot = Esc { h: mut x, tag: 0 }
    }
}
