struct HandlePair {
    h: mut u256,
    tag: u256,
}

fn mk_handle(_ x: mut u256) -> HandlePair {
    HandlePair { h: x, tag: 0 }
}

pub fn borrowck_call_result_aggregate_handle_then_write() {
    let mut value: u256 = 0
    let mut hp: HandlePair = mk_handle(mut value)
    value = 1
    hp.h = 2
}
