trait Bound {
    type Item
}

trait Foo<T> {}

struct Missing {}
struct S {}

impl Foo< <Missing as Bound>::Item > for S {}

fn needs_foo<T: Foo< <Missing as Bound>::Item >>(_value: T) {}

fn use_it(value: S) {
    needs_foo(value)
}
