struct Wrapper {
    p: Pair,
}

struct Pair {
    x: u32,
    y: u32,
}

fn unwrap(w: Wrapper) -> Pair {
    let p = w.p
    p
}

#[test]
fn example() {
    let w = Wrapper { p: Pair { x: 10, y: 20 } }
    let p = unwrap(w)
    assert(w.p.x == 10)
    assert(p.x == 10)
}
