use std::abi::sol

msg FooMsg {
    #[selector = sol("ping(uint256)")]
    Ping { value: u256 } -> u256,
}

pub contract Foo {
    recv FooMsg {
        Ping { value } -> u256 {
            return value
        }
    }
}
