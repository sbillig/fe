use core::ops::Not
use std::evm::effects::assert

#[test]
fn bool_not_trait_call() {
    let t = true
    let f = false

    let not_t = t.not()
    let not_f = f.not()

    assert(not_t == false)
    assert(not_f == true)
}
