struct Pair {
    a: u256,
    b: u256,
}

fn head(pair: ref Pair) -> u256 {
    pair.a
}

#[test]
fn test_reassigned_object_root_references_follow_rebinding() {
    let mut pair = Pair { a: 1, b: 2 }
    assert(head(ref pair) == 1)

    pair = Pair { a: 7, b: 8 }
    assert(head(ref pair) == 7)
}
