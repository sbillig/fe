[package]
name = "fe-native-rpc-runtime"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
alloy-primitives = "1.5.6"
reqwest = { version = "0.13.2", default-features = false, features = ["blocking", "json", "rustls"] }
serde_json = "1"
