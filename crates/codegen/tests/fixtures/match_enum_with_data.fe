
enum MyOption {
    None,
    Some(u64),
}

pub fn make_some(value: u64) -> MyOption {
    MyOption::Some(value)
}

pub fn match_option(opt: own MyOption) -> u64 {
    match opt {
        MyOption::Some(value) => value,
        MyOption::None => 0,
    }
}

pub fn main() -> u64 {
    let opt = make_some(7)
    match_option(opt)
}
