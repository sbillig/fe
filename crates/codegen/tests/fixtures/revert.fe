use core::ptr
use std::evm::{RawMem, RawOps}

fn fail() uses (ops: mut RawOps) {
    let data = ptr::alloc_bytes(32)
    ops.mstore(addr: data, value: 42)
    ops.revert(offset: data, len: 32)
}

pub fn main() uses (ops: mut RawOps) {
    fail()
}
