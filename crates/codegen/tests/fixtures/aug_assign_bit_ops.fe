pub fn aug_assign_bit_ops(x: u256, y: u256) -> u256 {
    let mut acc: u256 = x
    acc **= y
    acc <<= 3
    acc >>= 1
    acc &= 0xff
    acc |= 1
    acc ^= 2
    acc
}

pub fn main() -> u256 {
    aug_assign_bit_ops(3, 2)
}
