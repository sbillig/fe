use std::evm::RawOps

enum E { A, B }

fn f(e: E) uses (ops: RawOps) {
    match e {
        E::A => { ops.return_data(offset: 0, len: 0) }
        E::B => { ops.return_data(offset: 0, len: 0) }
    }
}

pub fn main() uses (ops: RawOps) {
    f(E::A)
}
