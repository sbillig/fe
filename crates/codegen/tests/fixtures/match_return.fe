use core::ptr
use std::evm::RawOps

enum E { A, B }

fn f(e: E) uses (ops: RawOps) {
    match e {
        E::A => { ops.return_data(data: ptr::MemBuffer::empty()) }
        E::B => { ops.return_data(data: ptr::MemBuffer::empty()) }
    }
}

pub fn main() uses (ops: RawOps) {
    f(E::A)
}
