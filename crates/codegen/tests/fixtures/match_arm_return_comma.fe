// Regression test: bare `return` in comma-separated match arms should parse correctly.

enum E {
    A(u256),
    B,
}

fn f(e: E) -> u256 {
    match e {
        E::A(v) => return v,
        E::B => return 0,
    }
}
