use core::ptr
use std::evm::RawOps

pub fn hash(data: *u8, len: u256) -> u256 uses (ops: RawOps) {
    ops.keccak256(data: ptr::MemBuffer::from_raw_parts(ptr: data, len: len))
}

pub fn main() -> u256 uses (ops: RawOps) {
    hash(ptr::alloc_bytes(32), 32)
}
