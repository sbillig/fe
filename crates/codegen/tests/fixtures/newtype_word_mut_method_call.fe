struct Wrap {
    inner: u256,
}

impl Wrap {
    fn bump(mut own self) -> Self {
        self.inner += 1
        self
    }
}

pub fn newtype_word_mut_method_call(x: u256) -> u256 {
    let w = Wrap { inner: x }
    let w = w.bump()
    w.inner
}

pub fn main() -> u256 {
    newtype_word_mut_method_call(1)
}
