trait Hashable {
    fn hash(self) -> u256
}

struct Cache<T: Hashable> {
    item: T,
    hash: u256,
}

impl<T: Hashable> Cache<T> {
    fn new(item: own T) -> Cache<T> {
        let hash = item.hash()
        Cache { item, hash }
    }
}
