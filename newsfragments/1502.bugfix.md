The borrow checker now tracks borrow handles stored in tuples, structs, arrays, and enum variants. Conflicting borrows while such a value is live are rejected, as is returning a value holding a borrow of a local.
