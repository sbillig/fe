Added first-class memory pointer support, including typed allocation helpers, pointer casts, dereference reads and writes, pointer field access, `MemArray<T>` memory arrays with explicit zeroing, and mutable indexing for pointer-backed arrays.
