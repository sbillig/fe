Generic function and method calls now honor trailing default type and const parameters when generic arguments are omitted, including dependent defaults and `Self` defaults on associated functions. Explicit generic argument lists may provide the required prefix while omitting a defaulted suffix. Generic const defaults are validated at their declarations, and default layout holes retain distinct call-site identities instead of causing layout aliasing or compiler panics.
