Added experimental host-native executable builds for standalone Fe files on
x86-64 and AArch64. Build Fe with the `cranelift` feature and pass
`--backend native` to produce an executable from `pub fn main() -> i32`.
