Fe now supports Rust-style closure syntax with capture-by-value semantics and `core::functional::Fn` calls. Closures currently accept a single parameter, cannot use effects or const contexts, and reject assignment to captured bindings with dedicated diagnostics.
