Fix compiler panics when storing named functions or tuple-variant constructors as values, and preserve trait dispatch when calling stored associated functions.

Preserve declared generic bounds when checking const defaults outside functions, and report errors instead of panicking on malformed default expressions.
