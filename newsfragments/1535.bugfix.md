Fix compiler panics when storing named functions or tuple-variant constructors as values, and preserve trait dispatch when calling stored associated functions.

Preserve declared generic bounds when checking const defaults outside functions, and report errors instead of panicking on malformed default expressions.

Resolve generic defaults in their declaration's scope and bounds, reject invalid type defaults and cyclic dependencies even when unused or overridden, and preserve dependent captures and independent layout-hole identities.

Correct default-hole declaration provenance to identify source parameters even when traits or functions have implicit generic prefixes.

Accept equivalent symbolic array lengths from generic defaults during semantic normalization without conflating different lengths.

Find inherent methods and associated functions on projection-constrained impl targets after binding receiver arguments, including targets with associated-type defaults.

Discover hidden layout and effect-provider generic slots before validating effect keys, preventing compiler query cycles for keys that combine associated constants and layout-hole defaults. Keep array and sibling layout-slot identities stable across deferred and zero-length extents.
