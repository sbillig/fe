Reserve `StorageMap`'s complete hashing buffer before invoking custom key encoders, preventing allocations during encoding from overwriting the key. Custom `StorageKey` implementations must now provide `encoded_len(self) -> u256` and make `write_key` return `()` instead of the encoded length.
