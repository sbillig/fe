Contract fields are now immutable by default unless declared with `mut`. Immutable fields must be initialized on every successful `init` path and are embedded into contract code; declare fields as `mut` to keep using storage-backed mutable contract state.
