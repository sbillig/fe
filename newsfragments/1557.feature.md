Replace semantic borrow checking with structural ownership and capability analysis, including stricter validation of moves through pointers, raw-memory effects, and native references. Preserve native reference addresses and layouts across slots and returns. Reserve StorageMap key-encoding buffers before invoking custom encoders; custom StorageKey implementations now require encoded_len.
